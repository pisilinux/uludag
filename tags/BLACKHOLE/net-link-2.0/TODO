- Handle kernel events
- Remove hardcoded "dnsmasq" package name from network scripts
- Move AccessPoint, Wireless, Profile classes under comar.network module
    - Also review/refactor them
- Put AdHoc support back into wireless-tools
- Start DHCP after associating to AP
    - When WEP authorization selected, try both hex and ASCII password
- Use ifplug
