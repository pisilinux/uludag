install2live
============

This is a hardcoded script that can be used to convert an installed Pardus distro into a Pardus Live CD image.

Dependencies:
============
squashfs-tools
syslinux

How to run
==========

$ sudo python build_live.py

It will build pardus.iso in the current directory

