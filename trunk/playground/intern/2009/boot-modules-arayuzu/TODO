- setup.py will be coded 
- i18n stuff ... 
- help docs will be written
- list functions will be called again after each operation. (remanining = 50% )

    ps: it will refresh "loaded modules" window when a module is loaded from "all available modules" window

    when we remove it is now refreshing. skip this.

- test, test, test...
