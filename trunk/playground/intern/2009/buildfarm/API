Provided Methods:

* appendToWaitQueue
    appends given package into WaitQueue
    returns True on success, False otherwise

* appendToWorkQueue
    appends given package into WorkQueue
    returns True on success, False otherwise

* getWaitQueue
    return current WaitQueue
    returns a list

* getWorkQueue
    return current WorkQueue
    returns a list

* removeFromWaitQueue
    removes given package from WaitQueue
    returns True on success, False otherwise

* removeFromWorkQueue
    removes given package from WorkQueue
    returns True on success, False otherwise

* transferToWaitQueue
    Transfers given package from WorkQueue to WaitQueue
    returns True on success, False otherwise

* transferToWorkQueue
    Transfers given package from WaitQueue to WorkQueue
    returns True on success, False otherwise

* updateRepository
    Updates local pspec repository
    returns a list

* buildPackages
    Build all packages in work and wait Queues
    returns True on success, WaitQueue otherwise

* buildIndex
    Generates binary repo's index
    returns True on success, False otherwise
