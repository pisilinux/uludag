A Web Interface for buildfarm based on the Django web framework
---------------------------------------------------------------

Basic functions:

# A developer will be able to transfer one or more binary package(s)
  (with its dependencies) from the test repo to the stable repo.


Possible extensions:

# Support for giving ACK/NACK online.
# Read-only operations on buildfarm
  (e.g. observing the queues)


