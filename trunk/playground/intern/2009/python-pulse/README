-------------python-pulse--------------
Experimental python bindings for pulseaudio
Author: Burak Alkan


in order to compile module;

$ python setup.py build

install pypulse.so to /usr/lib/python2.5/site-packages/

# python setup.py install

at last run main.py

$ ./main.py


Note: Use quit button on gui to close program for now, don't use [X] window button, otherwise loop won't die.
